---
phase: 01-command-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-command-verification/01-REPORT.md
autonomous: true
requirements:
  - FUNC-01
  - FUNC-02
  - FUNC-03
  - FUNC-04
  - FUNC-05
  - FUNC-06
  - FUNC-07
  - FUNC-08
  - FUNC-09
  - FUNC-10

must_haves:
  truths:
    - "01-REPORT.md 包含摘要表格，列出 FUNC-01 ~ FUNC-10 各項的 PASS/FAIL/PARTIAL 狀態"
    - "每個 FUNC-XX 有獨立段落，包含驗證方法、結果、證據引用"
    - "diff / status / skills:diff / to-repo --dry-run / to-local --dry-run 的實際輸出以 code block 呈現"
    - "程式碼引用使用行號格式（如 sync.js:1274-1311），不貼完整程式碼"
    - "報告中無使用者家目錄絕對路徑洩漏"
  artifacts:
    - path: ".planning/phases/01-command-verification/01-REPORT.md"
      provides: "Phase 1 完整體檢報告 — 指令功能正確性驗證"
      contains: "FUNC-01"
  key_links:
    - from: "01-REPORT.md"
      to: "sync.js"
      via: "行號引用"
      pattern: "sync\\.js:\\d+-\\d+"
---

<objective>
驗證 sync-ai 全部 6 個指令（diff / status / to-repo / to-local / skills:diff / skills:add）在正常操作路徑下的行為正確性，產出體檢報告 Section 1。

Purpose: 建立對同步工具功能正確性的信任基礎，確認不會造成設定遺失或損壞
Output: `.planning/phases/01-command-verification/01-REPORT.md`
</objective>

<execution_context>
@D:/code/sync-ai/.claude/get-shit-done/workflows/execute-plan.md
@D:/code/sync-ai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-command-verification/01-CONTEXT.md
@.planning/phases/01-command-verification/01-RESEARCH.md
@.planning/codebase/STRUCTURE.md

<interfaces>
<!-- 本 phase 不產生程式碼介面。報告結構由 D-04/D-05 定義。 -->

報告結構（per D-04, D-05, D-06）:
- 檔名: 01-REPORT.md
- 開頭: 摘要表格（FUNC-ID | 描述 | 結果 | 驗證方法）
- 正文: 每個 FUNC-XX 獨立段落含：
  - 驗證方法（靜態分析 / dry-run 實測）
  - 結果（PASS / FAIL / PARTIAL）
  - 證據引用（行號 + 摘要描述 + dry-run 輸出）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 驗證 FUNC-01 ~ FUNC-05（diff + to-repo 路徑）</name>
  <files>.planning/phases/01-command-verification/01-REPORT.md</files>
  <read_first>
    - sync.js（重點區段：第 20-81 行常數區、第 948-1002 行 buildSyncItems、第 1055-1132 行 diffSyncItems/applySyncItems、第 1274-1371 行 runDiff/runToRepo、第 831-910 行 settings 處理）
    - .planning/REQUIREMENTS.md（FUNC-01 ~ FUNC-05 定義）
    - .planning/phases/01-command-verification/01-RESEARCH.md（各 FUNC 追蹤路徑）
    - package.json（npm scripts 定義）
  </read_first>
  <action>
逐項驗證 FUNC-01 到 FUNC-05，方法如下：

**FUNC-01（exit code）— per D-01 靜態分析 + per D-02 dry-run 實測：**
1. 讀取 sync.js 常數區，確認 EXIT_OK=0 / EXIT_DIFF=1 的定義行號
2. 追蹤 runDiff() 的回傳路徑：hasDiff 為 false 時回傳 EXIT_OK，否則 EXIT_DIFF
3. 執行 `npm run diff`，觀察輸出，再用 `echo $?` 取得實際 exit code（注意：需分兩步執行，禁止 $() 命令替換）
4. 記錄 PASS/FAIL/PARTIAL 及證據

**FUNC-02（三類型差異顯示）— per D-01 + D-02：**
1. 追蹤 diffSyncItems() 三個 if 分支（settings / file / dir）的處理邏輯
2. 確認 buildSyncItems() 產出的五項 SyncItem 涵蓋三種 type
3. 從 `npm run diff` 實際輸出中確認各類型的顯示格式
4. 記錄結果

**FUNC-03（to-repo file 複製）— per D-01 靜態 + per D-02 dry-run：**
1. 追蹤 applySyncItems() 中 type === 'file' 分支 → copyFile() 呼叫
2. 確認 copyFile() 讀取 src 寫入 dest 的邏輯
3. 執行 `node sync.js to-repo --dry-run` 觀察 file 類型的預覽輸出（per D-03 不執行非 dry-run）
4. 記錄結果

**FUNC-04（to-repo DEVICE_FIELDS 剝離）— per D-01 靜態：**
1. 確認 DEVICE_FIELDS 常數值為 ['model', 'effortLevel']（sync.js 常數區）
2. 追蹤 mergeSettingsJson('to-repo') → loadStrippedSettings() → delete DEVICE_FIELDS 的邏輯
3. 確認 writeJsonSafe 寫入的是已去除 DEVICE_FIELDS 的物件
4. 記錄結果

**FUNC-05（to-repo 目錄鏡像）— per D-01 靜態 + per D-02 dry-run：**
1. 追蹤 applySyncItems() 中 type === 'dir' 分支 → mirrorDir() 呼叫
2. 確認 mirrorDir() 處理新增 / 更新 / 刪除三種情境
3. 從 to-repo --dry-run 輸出中確認 agents/ 與 commands/ 的預覽
4. 記錄結果

**輸出格式（per D-07, D-08, D-09）：**
- 行號引用格式：`sync.js:NNN-NNN`
- 每項含摘要描述讓讀者不需翻原始碼
- dry-run 輸出用 code block，標明「此為驗證時刻的快照」
- 確認輸出中無使用者家目錄絕對路徑（如有，手動遮罩為 `~/`）

先建立 01-REPORT.md 檔案，寫入標題與 FUNC-01 ~ FUNC-05 的驗證結果。摘要表格先填入這五項，FUNC-06 ~ FUNC-10 欄位留空待 Task 2 填入。
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.planning/phases/01-command-verification/01-REPORT.md','utf8'); const checks=['FUNC-01','FUNC-02','FUNC-03','FUNC-04','FUNC-05','PASS','sync.js:']; const missing=checks.filter(k=>!c.includes(k)); if(missing.length){console.error('Missing:',missing);process.exit(1)}else{console.log('All FUNC-01~05 sections present')}"</automated>
  </verify>
  <acceptance_criteria>
    - 01-REPORT.md 存在且包含 "FUNC-01"、"FUNC-02"、"FUNC-03"、"FUNC-04"、"FUNC-05" 字串
    - 每個 FUNC 段落含 "PASS" 或 "FAIL" 或 "PARTIAL" 字串
    - 至少出現 5 次 "sync.js:" 行號引用格式
    - 至少出現 1 個 code block（```）包含 dry-run 或 diff 實際輸出
    - 文件中不含 "C:\\Users\\" 或 "/home/" 等絕對路徑
  </acceptance_criteria>
  <done>FUNC-01 ~ FUNC-05 各有獨立驗證段落，含 PASS/FAIL/PARTIAL 結論、行號證據、dry-run 輸出佐證</done>
</task>

<task type="auto">
  <name>Task 2: 驗證 FUNC-06 ~ FUNC-10（to-local + dry-run + skills + status 路徑）</name>
  <files>.planning/phases/01-command-verification/01-REPORT.md</files>
  <read_first>
    - .planning/phases/01-command-verification/01-REPORT.md（Task 1 產出，接續編輯）
    - sync.js（重點區段：第 1372-1460 行 runToLocal/confirmAndApply、第 1467-1531 行 runSkillsDiff、第 1318-1322 行 runDiffAll、第 1774-1782 行 attachCommandHandlers、第 404-421 行 copyFile dry-run guard、第 470-510 行 mirrorDir dry-run guard、第 875-910 行 mergeSettingsJson dry-run guard）
    - .planning/REQUIREMENTS.md（FUNC-06 ~ FUNC-10 定義）
    - .planning/phases/01-command-verification/01-RESEARCH.md（各 FUNC 追蹤路徑）
  </read_first>
  <action>
逐項驗證 FUNC-06 到 FUNC-10，方法如下：

**FUNC-06（to-local 預覽確認）— per D-01 靜態 + per D-03 不實際執行：**
1. 追蹤 runToLocal() 流程：先 printToLocalPreview()，非 dry-run 時呼叫 confirmAndApply()
2. 確認 confirmAndApply() 內呼叫 askConfirm() 取得使用者確認後才 applySyncItems()
3. 執行 `node sync.js to-local --dry-run` 觀察預覽輸出格式（per D-02，dry-run 是安全的）
4. 記錄結果

**FUNC-07（to-local 保留 DEVICE_FIELDS）— per D-01 純靜態：**
1. 追蹤 mergeSettingsJson('to-local') 邏輯
2. 確認 deviceValues 提取（遍歷 DEVICE_FIELDS 從本機 settings 取值）
3. 確認最後 writeJsonSafe 寫入 { ...repo, ...deviceValues }（spread 順序確保本機欄位覆蓋）
4. 記錄結果

**FUNC-08（dry-run 零寫入）— per D-01 靜態 + per D-02 dry-run：**
1. 追蹤 copyFile 的 dryRun 守門（sync.js:410-413 附近）：dryRun 為 true 時只回傳比對結果
2. 追蹤 mirrorDir 的 dryRun 守門（sync.js:486-494 附近）：dryRun 時不呼叫 ensureDir/writeFileSync/rmSync
3. 追蹤 mergeSettingsJson 的 dryRun 守門（sync.js:888, 906 附近）：dryRun 時不呼叫 writeJsonSafe
4. 追蹤 runToRepo 中 dryRun 時設定 isWriting = false（sync.js:1345 附近）
5. 執行 `node sync.js to-repo --dry-run`，確認無檔案被修改（可用 git status 驗證）
6. 記錄結果

**FUNC-09（skills:diff 只輸出）— per D-01 靜態 + per D-02 實測：**
1. 追蹤 runSkillsDiff() 全函式，確認只有 console.log / readJson 呼叫
2. 確認無 spawnSync / writeFileSync / execSync 等副作用呼叫
3. 執行 `npm run skills:diff` 觀察輸出
4. 記錄結果

**FUNC-10（status = diff + skills:diff）— per D-01 靜態 + per D-02 實測：**
1. 追蹤 attachCommandHandlers() 確認 COMMANDS['status'].handler 綁定到 runDiffAll
2. 追蹤 runDiffAll() 確認依序呼叫 runDiff() + runSkillsDiff()
3. 執行 `npm run status` 觀察輸出是否同時包含設定 diff 與 skills diff 兩段
4. 記錄結果

**寫入 01-REPORT.md：**
- 將 FUNC-06 ~ FUNC-10 各段落追加到報告正文
- 更新摘要表格，填入 FUNC-06 ~ FUNC-10 的結果
- dry-run 輸出用 code block，標明快照時刻
- 確認全文無使用者家目錄絕對路徑洩漏
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.planning/phases/01-command-verification/01-REPORT.md','utf8'); const checks=['FUNC-06','FUNC-07','FUNC-08','FUNC-09','FUNC-10']; const missing=checks.filter(k=>!c.includes(k)); const hasAll10=[...Array(10)].every((_,i)=>c.includes('FUNC-'+(i+1).toString().padStart(2,'0'))); if(missing.length||!hasAll10){console.error('Missing:',missing,'hasAll10:',hasAll10);process.exit(1)}else{console.log('All FUNC-06~10 present, total 10 FUNCs confirmed')}"</automated>
  </verify>
  <acceptance_criteria>
    - 01-REPORT.md 包含 "FUNC-06"、"FUNC-07"、"FUNC-08"、"FUNC-09"、"FUNC-10" 字串
    - 每個 FUNC 段落含 "PASS" 或 "FAIL" 或 "PARTIAL" 字串
    - 全部 FUNC-01 ~ FUNC-10 十項均出現在報告中
    - 摘要表格包含十行資料列（每個 FUNC 一列）
    - 至少出現 3 個 code block 包含 dry-run 或指令實際輸出
    - 文件中不含 "C:\\Users\\" 或 "/home/" 等絕對路徑
  </acceptance_criteria>
  <done>FUNC-06 ~ FUNC-10 各有獨立驗證段落，摘要表格完整涵蓋十項 FUNC</done>
</task>

<task type="auto">
  <name>Task 3: 組裝最終報告 — 摘要、結論、風險標注</name>
  <files>.planning/phases/01-command-verification/01-REPORT.md</files>
  <read_first>
    - .planning/phases/01-command-verification/01-REPORT.md（Task 1+2 產出，最終潤飾）
    - .planning/REQUIREMENTS.md（確認十項需求全部涵蓋）
    - .planning/phases/01-command-verification/01-CONTEXT.md（確認符合 D-04 ~ D-09 格式要求）
  </read_first>
  <action>
最終潤飾與品質檢查（per D-04, D-05, D-06, D-07, D-08）：

1. **確認摘要表格完整（per D-05）：**
   - 報告最頂端（標題之後）有一張表格
   - 表格格式：`| FUNC-XX | 描述 | 結果 | 驗證方法 |`
   - 十列資料，每列對應一個 FUNC 需求
   - 結果欄位值為 PASS / FAIL / PARTIAL

2. **確認各段落格式（per D-04）：**
   - 每個 FUNC-XX 有獨立 `###` 或 `##` 層級標題
   - 段落含三個子項：驗證方法、結果、證據引用
   - 證據引用包含行號（per D-07）與摘要描述（per D-08）

3. **補充風險標注（Claude's Discretion）：**
   - 對任何標記為 PARTIAL 或 FAIL 的項目，補充風險說明與建議追蹤事項
   - 對 RESEARCH.md 中提到的 skills:diff lock 檔案路徑差異（repo 端 `skills-lock.json` vs 本機 `.skill-lock.json`），在 FUNC-09 段落加入觀察備註
   - 對 to-local 的 askConfirm TTY 依賴（Phase 2 範圍），在 FUNC-06 段落標注為「後續 Phase 2 追蹤」

4. **補充報告尾部：**
   - 加入「總結」段落，統計 PASS / FAIL / PARTIAL 各幾項
   - 加入「後續追蹤」段落，列出需要在 Phase 2/3/4 深入驗證的項目
   - 加入報告元資料（驗證日期、驗證環境、sync.js 版本）

5. **路徑安全檢查：**
   - 全文搜尋 `C:\Users\`、`/home/`、`/Users/` 等絕對路徑
   - 若有則替換為 `~/` 或 `<HOME>/`

6. **確認檔名與位置（per D-06）：**
   - 檔案位於 `.planning/phases/01-command-verification/01-REPORT.md`
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('.planning/phases/01-command-verification/01-REPORT.md','utf8'); const checks=['FUNC-01','FUNC-02','FUNC-03','FUNC-04','FUNC-05','FUNC-06','FUNC-07','FUNC-08','FUNC-09','FUNC-10','驗證方法','結果','總結']; const missing=checks.filter(k=>!c.includes(k)); const noLeak=!c.includes('C:\\\\Users\\\\')&&!c.includes('/home/'); if(missing.length||!noLeak){console.error('Missing:',missing,'noLeak:',noLeak);process.exit(1)}else{console.log('Report complete: all 10 FUNCs, summary, no path leak')}"</automated>
  </verify>
  <acceptance_criteria>
    - 報告開頭有摘要表格，包含 "FUNC-01" 到 "FUNC-10" 共十列
    - 每列含 "PASS" 或 "FAIL" 或 "PARTIAL" 字串
    - 報告含 "總結" 段落，統計各狀態數量
    - 報告含 "後續追蹤" 段落
    - 全文不含 "C:\\Users\\" 或 "/home/" 或 "/Users/" 絕對路徑
    - 檔案路徑為 .planning/phases/01-command-verification/01-REPORT.md
  </acceptance_criteria>
  <done>01-REPORT.md 完整包含十項 FUNC 驗證、摘要表格、總結、後續追蹤，格式符合 D-04~D-09 要求</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dry-run 指令輸出 → 報告 | 指令輸出可能含敏感路徑，需遮罩後才能寫入報告 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | 01-REPORT.md | mitigate | dry-run 輸出寫入報告前，遮罩所有 `C:\Users\*` 與 `/home/*` 絕對路徑為 `~/`；Task 3 最終全文掃描確認 |
| T-01-02 | Information Disclosure | dry-run 執行 | accept | dry-run 指令本身在本機執行，不傳輸資料；輸出僅存入 git-tracked 報告（已由 T-01-01 遮罩） |
| T-01-03 | Tampering | sync.js | accept | 本 phase 不修改任何程式碼，只讀取與執行 dry-run，無篡改風險 |
</threat_model>

<verification>
1. `01-REPORT.md` 存在於 `.planning/phases/01-command-verification/` 目錄
2. 報告包含 FUNC-01 ~ FUNC-10 全部十項驗證結果
3. 每項標記為 PASS / FAIL / PARTIAL
4. 摘要表格完整且格式正確
5. 行號引用格式 `sync.js:NNN-NNN` 出現至少 10 次
6. dry-run 輸出以 code block 呈現
7. 全文無使用者家目錄絕對路徑
8. `npm test` 通過（未破壞既有測試）
</verification>

<success_criteria>
- 01-REPORT.md 完整涵蓋 FUNC-01 ~ FUNC-10 十項需求驗證
- 每項有 PASS/FAIL/PARTIAL 結論、驗證方法說明、程式碼行號證據
- 報告可作為獨立文件閱讀，不需翻閱 sync.js 原始碼即可理解結論
- 無敏感路徑洩漏
</success_criteria>

<output>
After completion, create `.planning/phases/01-command-verification/01-01-SUMMARY.md`
</output>
